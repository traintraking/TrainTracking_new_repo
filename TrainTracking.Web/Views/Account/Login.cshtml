@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تسجيل الدخول - KuwGo</title>
    <script>
        (function () {
            const theme = document.cookie.split('; ').find(row => row.startsWith('theme='))?.split('=')[1];
            if (theme) {
                document.documentElement.setAttribute('data-theme', theme);
            }
        })();
    </script>
    <link href="/css/site.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #005eb8;
            --text-main: #2c3e50;
            --text-muted: #7f8c8d;
            --input-bg: #f8f9fa;
        }

        /* Sync with global theme variables if available */
        [data-theme="dark"] {
            --text-main: #e2e8f0;
            --text-muted: #94a3b8;
            --input-bg: rgba(255, 255, 255, 0.05);
        }

        body {
            font-family: 'Cairo', sans-serif;
            margin: 0;
            height: 100vh;
            display: flex; /* Flex container for split layout */
            overflow: hidden; /* Prevent scrolling on desktop if not needed */
            background-color: var(--bg-color, #f3f6f9);
            color: var(--text-color, #2d3748);
        }

        /* 1. The Form Side (Right) */
        .form-section {
            flex: 1;
            background: var(--card-bg, white);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px;
            overflow-y: auto; /* Allow scrolling if form is tall */
            position: relative;
        }

        /* 2. The Image Side (Left) */
        .image-section {
            flex: 1.2; /* Slightly wider */
            background-image: linear-gradient(rgba(0, 94, 184, 0.2), rgba(0, 94, 184, 0.4)), url('/images/login_train.png'); 
            background-size: cover;
            background-position: center;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            text-align: center;
            padding: 20px;
        }

        /* Content Container in Form Side */
        .auth-wrapper {
            width: 100%;
            max-width: 400px;
        }

        /* Branding in Image Side */
        .image-caption {
            max-width: 80%;
        }
        .image-caption h2 {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 20px;
            text-shadow: 0 2px 10px rgba(0,0,0,0.3);
        }
        .image-caption p {
            font-size: 1.2rem;
            opacity: 0.9;
            text-shadow: 0 1px 5px rgba(0,0,0,0.3);
        }

        /* Form Styling (Reuse clean style) */
        .brand-logo {
            height: 60px;
            margin-bottom: 30px;
            display: block;
        }

        .title {
            font-size: 28px;
            font-weight: 700;
            color: var(--text-main);
            margin-bottom: 10px;
        }

        .subtitle {
            font-size: 15px;
            color: var(--text-muted);
            margin-bottom: 40px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: var(--text-main);
            font-size: 14px;
        }

        .input-group {
            position: relative;
        }

        .form-control {
            width: 100%;
            padding: 16px 20px 16px 50px;
            border: 2px solid #eef2f7;
            background: var(--input-bg);
            border-radius: 12px;
            font-family: inherit;
            font-size: 15px;
            box-sizing: border-box;
            transition: all 0.3s;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary-color);
            background: white;
            box-shadow: 0 5px 15px rgba(0, 94, 184, 0.05);
        }

        .input-icon {
            position: absolute;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            color: #bdc3c7;
        }

        .form-control:focus + .input-icon {
            color: var(--primary-color);
        }

        .btn-primary {
            width: 100%;
            padding: 16px;
            background: var(--primary-color);
            color: white;
            border: none;
            border-radius: 12px;
            font-weight: 700;
            font-size: 16px;
            cursor: pointer;
            transition: 0.3s;
            font-family: inherit;
        }

        .btn-primary:hover {
            box-shadow: 0 10px 20px rgba(0, 94, 184, 0.2);
            transform: translateY(-2px);
        }

        .action-links {
            margin-top: 30px;
            text-align: center;
            font-size: 14px;
            color: var(--text-muted);
        }

        .action-links a {
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 700;
        }

        /* Responsive Design */
        @@media (max-width: 900px) {
            .image-section {
                display: none; /* Hide image on mobile/tablet */
            }
            
            .form-section {
                flex: 1;
                width: 100%;
            }

            body {
                overflow-y: auto; /* Allow body scroll on mobile */
            }
        }
    </style>
</head>
<body>

    <!-- Right Side: Form -->
    <div class="form-section">
        <div class="auth-wrapper">
            <img src="/images/logo.png" alt="KuwGo" class="brand-logo">
            
            <h1 class="title">مرحباً بعودتك! 👋</h1>
            <p class="subtitle">الرجاء إدخال بياناتك للمتابعة</p>

            <form asp-action="Login" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger small mb-3"></div>

                <div class="form-group">
                    <label class="form-label">البريد الإلكتروني</label>
                    <div class="input-group">
                        <input name="email" type="email" class="form-control" placeholder="example@mail.com" required dir="ltr" style="text-align: right;">
                        <i class="fas fa-envelope input-icon"></i>
                    </div>
                </div>

                <div class="form-group">
                    <label class="form-label">كلمة المرور</label>
                    <div class="input-group">
                        <input name="password" type="password" class="form-control" placeholder="••••••••" required dir="ltr" style="text-align: right;">
                        <i class="fas fa-lock input-icon"></i>
                    </div>
                </div>

                <div style="display: flex; align-items: center; margin-bottom: 30px;">
                    <input name="rememberMe" type="checkbox" id="rememberMe" style="width: 18px; height: 18px; margin-left: 10px; cursor: pointer;">
                    <label for="rememberMe" style="cursor: pointer; font-size: 14px; color: var(--text-muted);">تذكر هذا الجهاز</label>
                </div>

                <button type="submit" class="btn-primary">تسجيل الدخول</button>

                <div class="action-links">
                    ليس لديك حساب؟ <a asp-action="Register">أنشئ حساباً جديداً</a>
                </div>
            </form>
        </div>
    </div>

    <!-- Left Side: Image -->
    <div class="image-section">
        <div class="image-caption">
            <h2>KuwGo</h2>
            <p>رفيقك الذكي لرحلات القطار في الكويت. احجز، تتبع، وسافر بكل راحة.</p>
        </div>
    </div>

</body>
</html>
