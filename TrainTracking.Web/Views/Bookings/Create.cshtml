@model TrainTracking.Domain.Entities.Booking
@{
    // تأكد من جلب المقاعد المحجوزة من الـ ViewBag
    var takenSeats = (List<int>)ViewBag.TakenSeats ?? new List<int>();
}

<style>
    /* الاستايل الخاص بك بدون أي تعديل */
    .train-container {
        max-height: 600px;
        overflow-y: auto;
        padding: 20px;
        background: var(--bg-color);
        border-radius: 20px;
        border: 1px solid var(--border-color);
        transition: all 0.3s ease;
    }

    [data-theme="dark"] .train-container {
        background: rgba(15, 23, 42, 0.4);
    }

    .coach {
        background: var(--card-bg);
        border: 2px solid var(--border-color);
        border-radius: 15px;
        margin-bottom: 30px;
        padding: 20px;
        position: relative;
        box-shadow: var(--shadow-color);
        transition: all 0.3s ease;
    }

        .coach::before, .coach::after {
            content: '';
            position: absolute;
            left: 50%;
            width: 40px;
            height: 10px;
            background: var(--border-color);
            transform: translateX(-50%);
        }

        .coach::before {
            top: -12px;
            border-radius: 5px 5px 0 0;
        }

        .coach::after {
            bottom: -12px;
            border-radius: 0 0 5px 5px;
        }

    .coach-header {
        text-align: center;
        margin-bottom: 15px;
        color: var(--muted-text);
        font-weight: bold;
        font-size: 0.9rem;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .seat-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr) 40px repeat(2, 1fr);
        gap: 10px;
        justify-items: center;
    }

    .aisle {
        grid-column: 3;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--border-color);
        font-size: 0.8rem;
    }

    .seat {
        width: 45px;
        height: 45px;
        border: 2px solid var(--border-color);
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.2s;
        font-weight: 600;
        background: var(--card-bg);
        color: var(--text-color);
        font-size: 0.9rem;
    }

        .seat:hover:not(.taken) {
            border-color: var(--accent-color);
            color: var(--accent-color);
            background: rgba(0, 94, 184, 0.1);
            transform: scale(1.1);
        }

        .seat.selected {
            background: var(--accent-color);
            color: white;
            border-color: var(--accent-color);
            box-shadow: 0 0 10px rgba(0, 136, 255, 0.4);
        }

        .seat.taken {
            background: rgba(0, 0, 0, 0.05);
            color: var(--muted-text);
            cursor: not-allowed;
            border-style: dashed;
            opacity: 0.5;
        }

    [data-theme="dark"] .seat.taken {
        background: rgba(255, 255, 255, 0.05);
    }

    .legend {
        display: flex;
        justify-content: center;
        gap: 25px;
        padding: 15px;
        background: var(--card-bg);
        border-radius: 50px;
        margin-bottom: 25px;
        box-shadow: var(--shadow-color);
        border: 1px solid var(--border-color);
        flex-wrap: wrap;
    }

    .legend-item {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 0.85rem;
        color: var(--text-color);
    }

    .dot {
        width: 14px;
        height: 14px;
        border-radius: 4px;
    }

    .dot-booked {
        background: rgba(0, 0, 0, 0.05);
        border: 1px dashed var(--border-color);
    }

    [data-theme="dark"] .dot-booked {
        background: rgba(255, 255, 255, 0.05);
    }
</style>

@if (Model.Trip.Status == TripStatus.Completed)
{
    <div class="alert alert-danger text-center">
        🚫 هذه الرحلة مكتملة العدد
    </div>
}

<div class="container py-5">
    <div class="row">
        <div class="col-md-8">
            <div class="legend">
                <div class="legend-item"><div class="dot" style="border: 2px solid var(--border-color); background: var(--card-bg);"></div> متاح</div>
                <div class="legend-item"><div class="dot" style="background: var(--accent-color);"></div> اختيارك</div>
                <div class="legend-item"><div class="dot dot-booked"></div> محجوز</div>
                <div class="legend-item"><span class="badge bg-warning text-dark">VIP (2x)</span></div>
                <div class="legend-item"><span class="badge bg-info text-dark">First Class (1.5x)</span></div>
            </div>

            <div class="card shadow-sm border-0 mb-4 rounded-4 overflow-hidden bg-card">
                <div class="card-header bg-primary text-white py-3 border-0">
                    <h5 class="mb-0 fw-bold"><i class="fas fa-info-circle me-2"></i> تفاصيل المقطع المختار</h5>
                </div>
                <div class="card-body">
                    <div class="row align-items-center text-center">
                        <div class="col-5">
                            <div class="fw-bold text-primary fs-5">
                                @ViewBag.FromStation.Name
                            </div>
                            <div class="text-muted small">
                                @(((DateTimeOffset)ViewBag.SegmentDepartureTime).ToString("yyyy-MM-dd"))
                            </div>
                            <div class="fs-4 fw-bold text-muted">
                                @(((DateTimeOffset)ViewBag.SegmentDepartureTime).ToString("hh:mm tt"))
                            </div>
                        </div>

                        <div class="col-2">
                            <i class="fas fa-long-arrow-alt-left fa-2x text-muted"></i>
                        </div>

                        <div class="col-5">
                            <div class="fw-bold text-danger fs-5">
                                @ViewBag.ToStation.Name
                            </div>
                            <div class="text-muted small">
                                @(((DateTimeOffset)ViewBag.SegmentArrivalTime).ToString("yyyy-MM-dd"))
                            </div>
                            <div class="fs-4 fw-bold text-muted">
                                @(((DateTimeOffset)ViewBag.SegmentArrivalTime).ToString("hh:mm tt"))
                            </div>
                        </div>
                    </div>
                </div>
             </div>

            <div class="train-container">
                @for (int c = 0; c < ViewBag.totalSeats / 20; c++)
                {
                    // تحديد نوع العربة ومعامل السعر
                    string coachType = "Regular";
                    double multiplier = 1.0;
                    string coachClass = "";

                    if (c == 0) { coachType = "VIP (Double Price)"; multiplier = 2.0; coachClass = "border-warning shadow"; }
                    else if (c == 1) { coachType = "First Class (1.5x Price)"; multiplier = 1.5; coachClass = "border-info"; }

                    <div class="coach @coachClass" data-multiplier="@multiplier.ToString(System.Globalization.CultureInfo.InvariantCulture)">
                        <div class="coach-header @(c == 0 ? "text-warning" : (c == 1 ? "text-info" : ""))">
                            عربة @(c + 1) - @coachType
                        </div>
                        <div class="seat-grid">

                            @* s => number of seats *@
                            @for (int s = 1; s <= 20; s++)
                            {
                                int seatNum = (c * 20) + s;
                                bool isTaken = takenSeats.Contains(seatNum);

                                <div class="seat @(isTaken ? "taken" : "") @(c == 0 ? "border-warning" : (c == 1 ? "border-info" : ""))"
                                     data-num="@seatNum"
                                     data-multiplier="@multiplier.ToString(System.Globalization.CultureInfo.InvariantCulture)"
                                     onclick="selectSeat(this, @seatNum)">
                                    @seatNum
                                </div>

                                @if (s % 2 == 0 && s % 4 != 0)
                                {
                                    <div class="aisle"><i class="fas fa-ellipsis-v"></i></div>
                                }
                            }
                        </div>
                    </div>
                }
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm border-0 p-4 sticky-top bg-card" style="top: 20px; border-radius: 15px; border: 1px solid var(--border-color) !important;">
                <h5 class="fw-bold mb-4 text-adaptive">بيانات الحجز</h5>
                <form asp-action="Create" method="post" id="bookingForm">
                    <input type="hidden" asp-for="TripId" />
                    <input type="hidden" asp-for="FromStationId" />
                    <input type="hidden" asp-for="ToStationId" />
                    <input type="hidden" asp-for="Price" id="unitPrice" value="@Model.Price.ToString(System.Globalization.CultureInfo.InvariantCulture)" />                    <input type="hidden" name="selectedSeats" id="selectedSeatsInput" />

                    <div class="mb-3">
                        <label class="form-label small fw-bold" style="color:#6c757d;">اسم المسافر</label>
                        <input asp-for="PassengerName" class="form-control" placeholder="الاسم الكامل" required />
                    </div>

                    <div class="mb-3">
                        <label class="form-label small fw-bold" style="color:#6c757d;">رقم الهاتف</label>
                        <input asp-for="PassengerPhone" class="form-control" placeholder="01XXXXXXXXX" value="@ViewBag.UserPhone" type="tel" pattern="[0-9]{11}" maxlength="11" minlength="11" required /> 
                    </div>

                    @if (ViewBag.RewardTicketsCount > 0)
                    {
                        <div class="p-3 bg-warning-subtle border border-warning rounded-3 mb-3" style="background-color: rgba(255, 193, 7, 0.1);">
                            <div class="d-flex justify-content-between align-items-center">
                                <div>
                                    <i class="fas fa-ticket-alt text-warning me-2"></i>
                                    <span class="fw-bold text-dark-emphasis">لديك @ViewBag.RewardTicketsCount تذكرة خصم</span>
                                </div>
                                <div class="form-check form-switch">
                                    <input class="form-check-input" type="checkbox" name="useRewardTicket" id="useRewardTicket" value="true">
                                    <label class="form-check-label small" for="useRewardTicket">استخدام (خصم 50ج)</label>
                                </div>
                            </div>
                        </div>
                    }

                    <div class="p-3 bg-light-adaptive rounded-3 mb-4">
                        <div class="d-flex justify-content-between mb-2">
                            <span class="text-muted">المقاعد:</span>
                            <span id="txtSeats" class="fw-bold text-primary">--</span>
                        </div>
                        <div id="discountRow" class="d-flex justify-content-between mb-2 d-none">
                            <span class="text-danger">خصم تذكرة مكافأة:</span>
                            <span class="fw-bold text-danger">-50.000 EGP</span>
                        </div>
                        <div class="d-flex justify-content-between border-top pt-2">
                            <span class="fw-bold text-muted">الإجمالي:</span>
                            <span class="fw-bold text-success">EGP <span id="txtTotal">0.000</span></span>
                        </div>
                    </div>

                    <button type="submit" class="btn btn-primary w-100 py-2 fw-bold shadow-sm" id="btnSubmit" disabled>
                        تأكيد الحجز
                    </button>
                </form>
                <div asp-validation-summary="All" class="text-danger"></div>
            </div>
        </div>
    </div>
</div>

<script>
    let selectedSeats = [];
    const unitPriceElement = document.getElementById('unitPrice');
    const basePrice = parseFloat(unitPriceElement.value) || 0;
    console.log("Base Price Detected:", basePrice);
    const useTicketCheckbox = document.getElementById('useRewardTicket');
    if (useTicketCheckbox) {
        useTicketCheckbox.addEventListener('change', updateBookingInfo);
    }

    function selectSeat(el, num) {
        if (el.classList.contains('taken')) return;
        const multiplier = parseFloat(el.getAttribute('data-multiplier'));
        const index = selectedSeats.findIndex(s => s.num === num);
        if (index > -1) {
            selectedSeats.splice(index, 1);
            el.classList.remove('selected');
        } else {
            if (selectedSeats.length >= 3) {
                alert("الحد الأقصى هو 3 مقاعد");
                return;
            }
            selectedSeats.push({ num: num, mult: multiplier });
            el.classList.add('selected');
        }
        updateBookingInfo();
    }
    function updateBookingInfo() {
        const seatNumbers = selectedSeats.map(s => s.num);  
        document.getElementById('txtSeats').innerText = seatNumbers.length > 0 ? seatNumbers.join(', ') : "--";
        document.getElementById('selectedSeatsInput').value = seatNumbers.join(',');
        
        let total = 0;
        selectedSeats.forEach(s => {
            total += (basePrice * s.mult);
        });

        const discountRow = document.getElementById('discountRow');
        if (useTicketCheckbox && useTicketCheckbox.checked) {
            if (total > 0) {
                total = Math.max(0, total - 50);
                discountRow?.classList.remove('d-none');
            } else {
                discountRow?.classList.add('d-none');
            }
        } else {
            discountRow?.classList.add('d-none');
        }

        document.getElementById('txtTotal').innerText = total.toFixed(3);
        document.getElementById('btnSubmit').disabled = selectedSeats.length === 0;
    }
</script>